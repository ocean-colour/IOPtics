"""Algorithm specification and registry.

Declarative :class:`~ioptics.algorithms.spec.AlgorithmSpec` (a_nw/bb_nw model
choices, priors, RT toggles, fit method, noise model) plus a name -> spec
registry, seeded with ``expb_pow`` and ``giop`` in tandem.

Stub — implemented in Stage 2 (see
``docs/design/IOPtics_implementation.md`` §"Algorithm registry").
"""

from __future__ import annotations
