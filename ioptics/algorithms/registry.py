"""Name -> ``AlgorithmSpec`` factory; the growing registry.

Seeded with ``expb_pow`` and ``giop`` side by side (design: in tandem);
``register()`` adds more (e.g. ``gsm``).

Stub — implemented in Stage 2 (see
``docs/design/IOPtics_implementation.md`` §"Algorithm registry").
"""

from __future__ import annotations
