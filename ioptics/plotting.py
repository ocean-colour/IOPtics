"""Low-level static figure primitives (matplotlib), publication-styled.

Reuses ``bing.plotting`` helpers where they fit (the one soft exception to the
"no BING below prep/run/evaluate" rule).

Stub — implemented in a later stage (see
``docs/design/IOPtics_implementation.md`` §"Reporting").
"""

from __future__ import annotations
