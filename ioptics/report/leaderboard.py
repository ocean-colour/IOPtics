"""The persistent leaderboard, aggregated across sweep directories.

Ranks algorithms by headline metrics across (not within) sweep directories, so
no single global table is mutated.

Stub — implemented in a later stage (see
``docs/design/IOPtics_implementation.md`` §"Reporting").
"""

from __future__ import annotations
