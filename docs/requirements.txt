# Documentation build requirements for Read the Docs.
# Install with: pip install -r docs/requirements.txt
#
# The IOPtics package itself (and its runtime deps) is installed separately by
# Read the Docs via `.readthedocs.yaml` (pip install .), so this file only
# needs the Sphinx toolchain. Heavy / sibling imports (bing, ocpy, bokeh, ...)
# are mocked in conf.py, so they are not required to build the docs.

sphinx>=7.0
furo>=2024.1               # modern light/dark HTML theme (see conf.py)
